# Agent-status-shows-floating-window
Here is a floating window showing the agent status. It can display the programming status of your agent (such as Claude code) in real time. It always stays at the top and does not interfere with other tasks.
